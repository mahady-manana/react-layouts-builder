import { memoize } from './utils/js_utils.js';

const isFirefox = memoize(()=>/firefox/i.test(navigator.userAgent)
);
const isSafari = memoize(()=>Boolean(window.safari)
);

export { isFirefox, isSafari };
