import { ILayoutSection } from 'layouts-builder/interface';
export declare const checkNotFoundData: (layouts: ILayoutSection[], data: any[], key: string) => {
    layouts: ILayoutSection[];
    update: boolean;
};
